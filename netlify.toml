[[redirects]]
from = "/*"
to = "/index.html"
status = 404

[build]
command = "npm run build"
publish = "dist"
framework = "vite"

[functions]
directory = "netlify/functions"
node_bundler = "esbuild"

# Scheduled Functions
# Uncomment the section below after installing @netlify/plugin-scheduled-functions
# [[plugins]]
#   package = "@netlify/plugin-scheduled-functions"
#   [plugins.inputs]
#     # Run trial conversion daily at 2 AM UTC
#     [plugins.inputs.trial-conversion-cron]
#       schedule = "0 2 * * *"

